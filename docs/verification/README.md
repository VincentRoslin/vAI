# docs/verification — gate evidence

What was physically run/reviewed and observed, per `CLAUDE.md` Article IV. One
numbered file per verification event.

| # | File | Covers |
| - | ---- | ------ |
| 01 | `01_env_audit.md` | Phase 2 — dev machine (toolchains, GPU/VRAM, build tools, disk); Rust↔MSVC link chain |
| 02 | `02_phase3_probes.md` | Phase 3 — NVML per-process VRAM (unavailable), GGUF header parse (works), WDDM hang (inconclusive → Phase 15 watch) |
| 03 | `03_adversarial_review.md` | Phase 4 — assembled architecture + ~40-risk register + ADR changes |
| 04 | `04_phase5_crosscheck.md` | Phase 5.10 — binding docs ↔ ADRs ↔ plan ↔ ROADMAP consistency |
| 05 | `05_phase6_bootstrap.md` | Phase 6 — Tauri/React/Rust scaffold: check suite + app launch + IPC round-trip; startup baseline |
| 06 | `06_phase7_contracts.md` | Phase 7 — application contracts: `contracts/` module, `ts-rs` regen, round-trip + rejection sweep, `TokenDelta` perf probe |
| 07 | `07_phase8_config.md` | Phase 8 — configuration: `config/` module, layered defaults/file/session, migration, corrupt recovery, `config_*` IPC; startup baseline |
| 08 | `08_phase9_persistence.md` | Phase 9 — SQLite: `db/` module, writer/reader pools, `refinery` forward-only migrations + verified backup, transactions, `DbError`, corruption check; DB baselines |
| 09 | `09_phase10_observability.md` | Phase 10 — logging: non-blocking lossy writer, boundary secret redaction, ring buffer, config-driven reloadable level, `operation()` spans |
| 10 | `10_phase11_registry.md` | Phase 11 — model registry: `models/` module, `model_entry` table, CRUD + capability query, path confinement, computed availability, UUIDv4 ids |
| 11 | `11_phase12_acquisition.md` | Phase 12 — model acquisition: `acquisition/` module, reqwest download engine (resume/verify/register), budget guard, GGUF header parser, `/models` picker; gates 1/6/8 verified **live** (real Qwen 0.5B GGUF, 50.6 MB/s, HF SHA-256); gate 5 (fixed STT/TTS, ~5 GB) deferred → before Phase 18 |
| 12 | `12_phase13_resources.md` | Phase 13 — resource manager: `resources/` module (`HardwareProbe` NVML/`sysinfo`/mock, closed-form VRAM estimate + EMA calibration, reservation ledger, `request`/`commit`/`observe`/`release`/`reconcile` behind one async `Mutex`); config schema v4 (`vram_safety_margin_mb`); all 9 gate items mock-verified + real probe confirmed |
| 13 | `13_phase14_lifecycle.md` | Phase 14 — model lifecycle manager: `lifecycle/` module (`ModelBackend`/`LoadedInstance` traits + `FakeBackend`; state machine `Unloaded→Loading→Loaded⇄Busy→Unloading` + `Failed` recovery behind one async `Mutex`; coalesced loads; Phase 13 reservation held across the load; bounded retry; ~2 s liveness monitor). `ModelState::Busy` added additively. All 9 gate items pass (15 tests, fake backend) |
| 16 | `16_phase17_engine.md` | Phase 17 — conversation engine: `ConversationService` → `ConversationEngine` (rename, single path); `send` split into `add_user_turn(typed content)` + `generate`; explicit `GenerationState` (`Idle`/`Generating{task,convo}`) + `chat_state` IPC; `render_chatml` also renders transcribed audio. All 6 gate items pass — Phase 16 live gate re-run on the re-hosted engine, TTFT ≈ 44 ms (no regression). |
| 15 | `15_phase16_slice.md` | Phase 16 — first vertical slice / text chat: `conversation/` module (repo + ChatML prompt + service), V0004 migration, `chat_*` IPC + streaming Channel, rebuilt `ChatVoice.tsx`. Concurrency = reject. **All 9 gate items pass** — a live Rust test runs the whole stack (register → real `llama-server` load → send → stream "pong" → persist → restart recovery → cancel → reuse); end-to-end TTFT ≈ 33 ms. First real product milestone. |
| 14 | `14_phase15_llama.md` | Phase 15 — llama.cpp adapter: `llm/` module (`LlamaBackend`/`LlamaServer`, `server` free-port + `--api-key` bearer + Windows Job Object, `client` `/health` + `/completion` non-stream + SSE stream with deadline + cancel, `protocol` = the only llama.cpp JSON). 13 stub tests + **15.D run live** on the RTX 5080 with a pinned prebuilt (`b10819`, CUDA 13.3, ADR-0004 amended) + real Qwen 0.5B: **all 9 gate items pass** — load ~775 ms, TTFT ≈ 23 ms, ≈ 278 tok/s, cancel frees slot, kill detected, no orphan. Config schema **v5** (`runtimes.dir`) |
| 21 | `21_phase21_memory.md` | Phase 21 — persistent per-Persona memory: `memory/` module (`repo` — `memory` + a standalone `memory_fts` FTS5 table, `V0006`, one-tx sync, prune-at-cap, BM25 scope-filtered `search` · `extract` — post-turn `llm.generate` → strict JSON → importance≥3 + Jaccard-dedup + length gate → store w/ provenance · `retrieve` — deterministic sorted-unique FTS query · `MemoryService` — pure-read retrieval, `Semaphore(1)` fire-and-forget extraction, recall-gap log). `MemoryId` · `contracts::memory` (`MemoryKind`, `Memory`). Context builder gains a `max_memory_tokens` sub-budget (0.15×ctx, additive). Engine retrieves per-Persona (FR-56) + spawns extraction on a clean completion. `memory_list` / `memory_delete` IPC + a Settings list. **All 7 gate items pass** — 16 `memory` tests + 3 `context::builder` memory tests + 2 `conversation` end-to-end. No new ADR (ADR-0012 finalisations recorded). 328 rust, 15 vitest. |
| 20 | `20_phase20_personas.md` | Phase 20 — personas & the one context builder: `context/` module (`persona` + `PersonaRepo` + `V0005` · `sanitize::strip_control` SECURITY C2 · `tokens::estimate_tokens` no-dep heuristic · `builder::ContextBuilder` — deterministic ChatML, shed order memory→persona-truncation→never base system, `total_tokens ≤ budget` by construction, `Provenance` logged `target:"context"`). `Conversation.persona_id` (additive) · `PersonaId` · `conversation::prompt` **deleted** · `preview_prompt` + `chat_prompt_preview` (FR-34) · `persona_*` IPC · Settings form + ChatVoice picker/"Show prompt". **All 6 gate items pass** — 24 `context` tests + a `conversation` prompt-carries-persona assertion + `set_persona` fixed-after-turn. No new ADR. |
| 19 | `19_phase19_voice-out.md` | Phase 19 — voice-out: `voice/chunker` (streamed deltas → clauses), `voice/playback` (`cpal` output + PCM queue + instant `stop`), `voice/resample::Resampler16`, `voice/tts::TtsOutput` (Chatterbox worker + playback, ~1 clause lookahead). `VoiceState::{Thinking,Speaking,Interrupting}`; `start_listening(id, model_id: Option)` → the listen→think→speak→listen loop; **barge-in** (VAD onset after a 500 ms grace, or stop) → `engine.cancel` (persists partial, `stop_reason=Cancelled`) → `tts.cancel` → `playback.stop` → `Listening`, timed. Config **v7** (`voice.output_device`). `workers/tts.py` = Chatterbox Turbo; venv gains `torch 2.11.0+cu128` (overrides the chatterbox pin). **All 7 gate items pass** — 30 voice unit tests + a **live** gate on the RTX 5080: 3 clauses spoken, warm RTF ≈ 0.45, **barge-in trigger→silence ≈ 4 ms**. |
| 18 | `18_phase18.5_deploy-diagnostics.md` | Phase 18.5 — deploy & diagnostics (owner-requested insert, pulled from P37): `scripts/deploy-local.mjs` (release build + launch in place), `logging::enable_file_sink` (rotating daily redacted JSON-lines under `<app_data>/logs/` + `sweep_logs`), `diag_export` snapshot (build / config / registry / resources / lifecycle / conversation metadata / recent logs / host — redacted, no content), a Settings button, `build.rs` git-SHA, UI breadcrumbs. **All 7 gate items pass** — release build verified (2m31s), file sink verified against the real binary (redacted, no secrets), diag/Settings unit + component tested. All local (ADR-0015). |
| 17 | `17_phase18_voice-in.md` | Phase 18 — voice-in: `worker/` (the shared stdio JSON-lines supervisor — Job Object, `WorkerHello` version check, `WorkerJobId` mux, ADR-0015 env, bounded restart) + `voice/` (`cpal` capture, `rubato` → 16 kHz mono, Silero v5 VAD via `ort`, segment WAV, `VoiceInput` → `add_user_turn(Text)`). `workers/stt.py` = faster-whisper large-v3 fp16 CUDA; `.venv` (**ADR-0018**); config schema **v6** (`workers.*`, `voice.input_device`); `voice_*` + `chat_generate` IPC; push-to-talk UI. **All 7 gate items pass** — 25 unit tests (real VAD + stdlib fake worker) + a live capture→transcript gate on the RTX 5080 ("Hello local AI, this is a voice input test." → one user turn; RTF well under 1.0). |

Later phases add their gate evidence here (the performance/offline/fault/security/
dependency/maintainability audits, etc.).
