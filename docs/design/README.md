# docs/design — visual design

| File | What |
| ---- | ---- |
| `visual-language.md` | Soft lock — the shell layout, chat geometry, and the radius/spacing scale every screen builds against; the right panel + per-tab layouts left flexible per feature phase |

Elaborates `UI_GUIDELINES.md` (the binding UI standard). A full visual overhaul is
expected later; this exists so the first real screens (Phase 16+) build against a
consistent baseline.
