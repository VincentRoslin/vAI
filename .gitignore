# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
src-tauri/target/
src-tauri/gen/schemas/

# NOTE: src/bindings/ (generated by ts-rs) IS committed — the IPC contract is a
# reviewable artifact. The check script regenerates it and fails on a diff.

# Logs / local
*.log
npm-debug.log*
*.local

# Large runtime assets kept in-project (owner preference) — NOT committed.
# llama-server(.exe) + CUDA DLLs (~540 MB); downloaded GGUF + STT/TTS/VAD models.
/runtime/
/models/

# Python venvs. Recreate both from scripts/setup-venv.mjs (ADR-0018):
#   /.venv/       — workers (STT/TTS): faster-whisper + CUDA 12 libs + torch 2.11
#   /.venv-image/ — image sidecar only: diffusers + Krea 2 deps (two-venv split,
#                   ADR-0018 amendment 2026-09-06 — chatterbox-tts hard-pins
#                   torch/transformers/diffusers against Krea 2's needs).
/.venv/
/.venv-image/

# Editor
.idea/
.DS_Store
*.sw?

# Keep .vscode/extensions.json, ignore the rest
.vscode/*
!.vscode/extensions.json
